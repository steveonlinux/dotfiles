####AUTOSTART#### ./scr/catsee.sh neofetch export SHELL="zsh" export PF_INFO="ascii title os kernel wm shell uptime pkgs memory " export PF_COL3=3 export PF_COL1=2
#pfetch
fastfetch --logo arch3
####ALIASES####
alias cp="cp -i"                          # confirm before overwriting something
alias df='df -h'                          # human-readable sizes
alias free='free -m'                      # show sizes in MB
alias np='nano -w PKGBUILD'
alias more=less
alias ls="exa -l"
alias la="exa -la"
alias rm="rm -i"
alias reb="doas reboot now"
alias re="doas shutdown now"
alias pacs="doas pacman -Syu"
alias pacR="doas pacman -R"
alias pacS="doas pacman -S"
alias pacQ="doas pacman -Q"
alias bashrc="nvim ~/.bashrc"
alias qt="nvim ~/.config/qtile/config.py"
alias pia="cd / && ./opt/piavpn/bin/pia-client && disown"
alias pi="ssh pi@192.168.86.240"
alias dm="bash ~/Downloads/Github_Clones/dmscripts/dmconf"
alias redO="redshift -O 3000"
alias redx="redshift -x"
alias pk="pulseaudio -k"
alias pst="pulseaudio --start"
alias scr="scrot --select"
alias nv="neovide"
alias b13="ssh stephen.marks5309@bmit-compile.kccd.edu"
alias compb13="cd ~/Documents/Spring_2021/COMP_B13"
alias icat="kitty +kitten icat"
#alias feh="devour feh"
#alias mpv="devour mpv"
#alias gparted="devour doas gparted" 
#alias nv="devour neovide"
#alias zat="devour zathura"
alias left="cd $HOME/.config/leftwm/"
alias poly="devour neovide $HOME/.config/leftwm/themes/basic_polybar/polybar.config"
alias bt="bluetoothctl"
alias bl="doas bl"
alias pacsize="expac -s \"%-30n %m\" -H M | sort -rhk 2 | less" 
alias deorphan="doas pacman -Qtdq | doas pacman -Rns -"
alias gsO="gammastep -P -O 3000 & disown"
alias gsx="gammastep -x & disown"
alias csubarm="ssh Stephen2238@fenrir.cs.csubak.edu"
alias odin="ssh smarks@odin.cs.csubak.edu"

alias ss='hyprctl -j activewindow | jq -r '\''(.at[0]|tostring) + "," + (.at[1]|tostring) + " " + (.size[0]|tostring) + "x" + (.size[1]|tostring)'\'' | grim -g - screenshot.png'
alias mull='mull.sh'
alias power='doas power.sh'
alias killorphans='doas pacman -Rns $(pacman -Qdtq)'
alias lv='lvim'

alias site='cd /home/steve/scr/personal_site/steve_site'

####PATHS####
export PATH="$PATH:/home/steve/Documents/Spring_2021/COMP_B13"
export PATH="$PATH:/home/steve/bin"
export PATH="$PATH:/home/steve/.local/bin"
export PATH="$PATH:/home/steve/.emacs.d/bin"
export PATH="$PATH:/home/steve/Scripts/catsee"
export PATH="$PATH:/home/steve/.cargo/bin"
export PATH="$PATH:/home/steve/scr"

####MANPAGER####
#export MANPAGER="nvim -c 'set ft=man' -"
#export MANPAGER="/bin/sh -c \"col -b | vim -c 'set ft=man ts=8 nomod nolist nonu noma' -\""
export PATH=/home/$USER/.config/nvcode/utils/bin:$PATH
export MANPAGER='lvim +Man!'

####XDG BASE DIRECTORY WORKAROUNDS####
#https://wiki.archlinux.org/title/XDG_Base_Directory
export ANSIBLE_HOME="${XDG_CONFIG_HOME}/ansible"
export ANSIBLE_CONFIG="${XDG_CONFIG_HOME}/ansible.cfg"
export ANSIBLE_GALAXY_CACHE_DIR="${XDG_CACHE_HOME}/ansible/galaxy_cache"

####ARCHIVE EXTRACTOR####
ex ()
{
  if [ -f $1 ] ; then
    case $1 in
      *.tar.bz2)   tar xjf $1   ;;
      *.tar.gz)    tar xzf $1   ;;
      *.bz2)       bunzip2 $1   ;;
      *.rar)       unrar x $1     ;;
      *.gz)        gunzip $1    ;;
      *.tar)       tar xf $1    ;;
      *.tbz2)      tar xjf $1   ;;
      *.tgz)       tar xzf $1   ;;
      *.zip)       unzip $1     ;;
      *.Z)         uncompress $1;;
      *.7z)        7z x $1      ;;
      *)           echo "'$1' cannot be extracted via ex()" ;;
    esac
  else
    echo "'$1' is not a valid file"
  fi
}
